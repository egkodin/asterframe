name: Restore complete source

on:
  push:
    branches: [main]
    paths:
      - '.bootstrap/**'
      - '.github/workflows/bootstrap.yml'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: asterframe-full-restore
  cancel-in-progress: false

jobs:
  restore:
    if: ${{ github.actor != 'github-actions[bot]' }}
    runs-on: ubuntu-latest
    steps:
      - name: Check out restoration commit
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Assemble, validate, and publish Asterframe
        run: bash .bootstrap/bootstrap.sh
